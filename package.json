{
  "name": "prj_chat_realtime",
  "private": true,
  "workspaces": [
    "web",
    "mobile"
  ],
  "scripts": {
    "dev:web": "npm --workspace web run dev",
    "dev:mobile": "npm --workspace mobile run start",
    "typecheck": "npm --workspace web exec tsc --noEmit && npm --workspace mobile exec tsc --noEmit"
  }
}
